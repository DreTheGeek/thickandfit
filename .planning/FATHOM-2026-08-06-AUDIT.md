# What she asked for on 2026-08-06, and what actually exists

Source: Fathom recording `171093734` (2026-08-06), summary + full transcript, 376 segments, read end
to end. Carry-forward checked against `166801787` (2026-07-23) and `168975085` (2026-07-30, internal,
no Stephanie).

Every status below was established by reading the code or querying the production database, not by
reading our own planning docs. Where a planning doc and the call disagree, the call wins and the doc
is named as wrong.

Attendees and owners: **Stephanie** (client), **Shakira** (funnel copy + design), **Rodney**
(scope, client ops), **LaSean** (build). LaSean presented this codebase as his work on the call, so
every "LaSean" item lands here.

---

## The launch date. Answer: October. No specific day was ever named.

Our docs say Sept 27 in more than twenty places. That is dead.

> **Shakira** [06:...]: "the actual doors opening can open on your birthday or we can change it if
> you want to move it earlier."
> **Stephanie** [07:39]: "Like the doors of what? I'm a little confused. **I thought we agreed on
> October.**"
> **Shakira**: "Oh, we're going to do Libra and not your birthday month."
> **Stephanie**: "We're going to do Libra season. because remember, we're still in the process of
> getting the app actually approved in the app store."
> **Shakira** [08:00]: "on the 27th, that's when they would be able to access the app, **but we can
> do October**."
> **Stephanie** [08:07]: "**I think that's going to be the smartest thing logistic-wise.**"

**Concluded: October.** Not concluded: which day in October. Libra season is Sept 23 to Oct 22, so
"October, Libra season" leaves a 22-day window and nobody closed it. That is the first question for
the next call, and it blocks the giveaway rules (which cannot say "starts today"), Shakira's copy,
and the founding-window arithmetic.

What still hardcodes Sept 27:

| where | what it says |
|---|---|
| `src/lib/billing/offer.ts:27` | `DOORS_OPEN_ISO = '2026-09-27T07:00:00.000Z'` |
| `src/messages/en.json:2376` | "get in first on my birthday, Sept 27" (live on the site) |
| `src/messages/es.json:2376` | "27 de septiembre" (live on the site) |
| `src/lib/admin/draw-types.ts:31` | `main: 'Main prize (doors open, Sept 27)'` |
| `src/lib/admin/waitlist-metrics.ts:327` | `doorsOpenIso: '2026-09-27T00:00:00Z'` |
| `src/app/(admin)/admin/launch/page.tsx` | the whole runway board counts down to Sept 27 |
| `CLAUDE.md`, Site Visibility | "open Sept 27" for `PRELAUNCH_HIDE_SITE` |
| `.planning/GIVEAWAY-RULES-DRAFT.md:25` | "the main one on your birthday Sept 27" |

---

## The gap list

Ordered by how much it costs to leave alone. Anything already done is not listed unless she asked
for it by name on this call.

### 1. The filming checklist is built from someone else's exercises. She has asked twice.

**She said** [29:59]: "I noticed that a lot of exercises and the checklist were things that I didn't
do. So I was hoping to add in the checklist of already what I had in Linus... **that's what I used
today to film my actual exercises**." And again [57:22]: "in the exercise library that I have on
Linus, when you toggle my exercises, it's going to show you all the ones that I own, **which is
371**... I wanted those specific names to be put on that sheet so I can check off."

**Owner:** Rodney committed ("we'll work on that over the next day or two"). Build is ours.

**Status: not started.** Proven against production:

```
select count(*) filter (where is_core and is_coach_authored)      -->   1
       count(*) filter (where is_core and not is_coach_authored)  -->  75
       count(*) filter (where is_coach_authored and not is_core)  --> 366
from exercises
```

367 rows are hers (366 carry a `lenus_video_key`). 76 rows are `is_core`. **Exactly one row is
both.** So 75 of the 76 movements she is filming from are not hers, and 366 of hers are not on the
list. `.planning/FILMING-SHOT-LIST.md` and the artifact she was sent are both generated by
`.qa-visual/shot-list.mjs`, whose query is `where e.is_core`.

**Missing:** change the predicate to her authored set, re-cut the blocks over 367 rows instead of 76,
and decide what happens to the 26 glute movements added on 2026-08-03 that she may not program (the
document already flags that as her call and she has not been asked).

### 2. The Chrome checklist bug is real, and the artifact cannot save anything at all.

**She said** [58:17]: "when I try to input that checklist on Google Chrome, it doesn't show me what
I've already completed... **it completely put it at 0%. It doesn't save what I complete.**"

**Owner:** us. Shakira answered "Okay. That'll fix it."

**Status: confirmed, unfixed, and worse than reported.** The artifact she was sent is
`your-filming-list.md` (`claude.ai/code/artifact/698038cb-2649-4d71-b1a7-c6046dfe34ea`, updated
2026-08-04). Fetched and read: it is a static Markdown table. **There are no checkboxes in it.** The
line she saw is hard-coded from the database, `"76 movements, 0 filmed."`, computed from
`video_mux_id is not null`, and only 2 of 1,259 exercise rows have one. So it will print 0 forever,
in every browser, no matter how much she films.

There is a second candidate with the same symptom: `.planning/steph-plan.html` does have tickable
tasks and a percent bar, but it persists to `localStorage` under key `tf-steph-plan-v1`, which is per
browser and per device. Tick in Safari, open in Chrome, get 0%. Her description fits either.

**Missing:** a filming checklist that persists server-side and is keyed to her account, not to a
browser. `/coach/exercises` has no filmed flag and there is no filming surface in the app at all. As
long as this is a document, it will keep resetting.

### 3. Mid-ticket is $299. Our live copy says $200.

**She said** [15:21]: "I've decided to stick to $2.99 for mid ticket" (transcription of $299).
**Rodney:** "I like that. $2.99 for mid ticket."

**Status: not done.** `$299` appears nowhere in the codebase. `src/messages/en.json:557` sets
`tierTeamPrice` to **"From $200/mo"** and that string renders on step 4 of onboarding, in front of
every new member. `src/lib/billing/tiers.ts` still describes the tier as "$200-300". The live number
is wrong by up to $99 a month.

### 4. Legacy clients grandfathered. Nothing exists to grandfather them with.

**She said** [1:09:00 exchange]: her longest client pays $275/mo; **Rodney**: "she can be like a
legacy grandfather then member type deal"; and the rule, **Rodney** [1:09:16]: a price increase only
if "they start reaching out to you and start asking for more... access to the new stuff."

**Status: not started.** No grandfather flag, no per-member price, no legacy plan concept. Production
`entitlements` holds **1 row**. This is genuinely blocked on her side: it needs the colour-coded
client list with renewal dates and transition notes that Rodney asked for and she has not returned.

### 5. Giveaway cadence: every Tuesday and Thursday, escalating. We built three named drawings.

**Shakira** [05:...]: "it just tells every Tuesday and Thursday until your birthday that you're going
to be giving prizes each week. **It gets bigger.**"

This was also decided on 2026-07-23 ("drawings twice weekly, e.g. Tuesdays and Thursdays"). Two calls,
same answer, still not built.

**Status: not started.** `grep` for Tuesday/Thursday across the funnel returns nothing. What exists
is `DrawKind = 'early_bird' | 'main' | 'founding_final' | 'bonus'`, labelled "around Aug 18", "doors
open, Sept 27", and "window close". `.planning/GIVEAWAY-RULES-DRAFT.md` asks her to confirm three
drawing dates, which is the wrong question to put in front of her. **That document contradicts the
call and should be rewritten before she reads it.**

**Missing:** a recurring twice-weekly schedule, a prize ladder that escalates week over week, and an
end date, which is now blocked on the October date above.

### 6. The 20-entry cap is wrong. We promise 20 and pay 63.

**Shakira** [13:47]: "**The most number of entries they'll be able to get is 20.**"

**Status: built wrong.** `src/lib/funnel/service.ts:75` is `REFERRAL_CAP = 20`, which caps
*referrals*, not entries. With `ENTRY_POINTS = { signup: 1, quiz: 2, referral: 3, follow: 1 }`, the
ceiling is 1 + 2 + (20 x 3) + follows = **63 or more**. The live fineprint reads "3 more giveaway
entries (capped at 20)", which any reader takes as 20 entries. The 2026-07-23 summary recorded the
same phrase, "capped at 20 total entries", so the misreading is old.

This matters beyond arithmetic: entry counts are the weights in the draw, and the draw is
publishable and verifiable by design. A cap that does not match the published rules is the one thing
that makes a weighted draw indefensible.

### 7. The quiz is 5 questions, not 8, and it is missing the one that matters.

**Shakira** [09:37]: "it asks eight questions... each one of these responses is going to be tagged on
the back end."

**Status: partly.** `src/components/funnel/quiz-form.tsx` asks five: goals (multi), home/gym/both,
days per week, how they eat, language. Its own header comment says "five questions, one screen".

Missing three of Shakira's eight: where they are starting from, what has got in the way before, and
**"when the app opens, what are you looking for"**. That last one is the tier-intent question. It is
the entire reason the call spent ten minutes on quiz tagging, and it is the question that carries the
next item.

### 8. "Serious 1-on-1" should read "Work exclusively with Coach Steph".

**She said** [11:53]: "how would that be decipher from Serious 101? Because I'm thinking Serious 101
**work exclusively with Coach Steph**. Because, you know, we're going to have that mid-ticket where
they could be working with my assistant and I'm the one facilitating the programming." And [12:22]:
"like, really, like, decipher that, like, **work exclusively with Coach Steph**."

**Owner:** Shakira for the wording, us for the build.

**Status: not started, and there is nowhere to put it.** The question that option belongs to does not
exist in our quiz (see item 7). The nearest live strings are onboarding tier labels: `tierStephName`
= "1-on-1 with Coach Steph" and `tierStephShort` = "1-on-1 with Steph". Both are the tier picker, not
the quiz, and neither uses her wording. She also asked for a preface making clear she writes the
programming even when Daniela delivers it; no such line exists anywhere.

### 9. Meal plans by tier. Low tier has no recipe library and no calculator to open.

**She said** [23:46]: "There'll be access to the low ticket, being able to use the macro calculator
in order to find their macros. Then they're going to have access to cookbooks, the recipes... **they
got to figure out their own meal plan, but I'm going to give you your macros, but I'm not doing your
meal plan. That's for clients, for Daniela, and obviously myself.**"

**Status: partly, and not gated.** Production has 158 recipes and 4 recipe books, but every recipe
route is under `/coach/tool/`. **There is no member-facing recipe library route** (`find src/app`
excluding coach returns nothing for recipe, macro or calculator). A low-tier member only ever meets a
recipe as a "pick one" option inside an assigned meal plan, which by this decision she will never
have. So the low tier's two promised features are, today, invisible to her.

There is also no tier gate. `/nutrition/plan` calls `requireEntitled()`, which is binary. The only
thing keeping a $19.97 member away from a custom plan is that nobody assigned her one.

### 10. Mid-tier custom plan on a 2-day delay. Not built.

**LaSean** [24:34]: "when they automatically sign up for the mid ticket, do we want to give it like a
**two day wait** so we can learn them?" **Stephanie** [25:22]: "you hit the nail on the head... we
want to give that humanized feeling... **it's important for the system to think like me and not
promote as AI**."

**Status: not started.** No delay, no scheduler, no generator. `delay`, `deliver_at`, `48`, `2 day`
return nothing across `src/lib/coach/meal-plans.ts` and `meal-plan-actions.ts`. The 157 `meal_plans`
rows in production are all hand-built through the coach builder. Whatever plan generation she was
shown on the call, it does not exist in this repo.

### 11. The 78-question portal. Built. Zero answered. She could not find it.

**LaSean** [21:41]: "one of the biggest things that I'm going to need from you is whenever you get
inside the... 78 questions." **Stephanie** [21:49]: "**I did, and it looked like gibberish.** sorry.
Not tech savvy at all." **LaSean** [1:06:00 area]: "I'll go in and screenshot it and send everything
to you exactly how I need to be done."

**Status: built, unused, and the handholding was never sent.** `src/lib/coach/interview-bank.ts`
holds exactly **78** prompts across 6 sections (Philosophy & Voice, Nutrition Method, Training
Method, Client Psychology, Real Client Scenarios, Safety & Referrals). Production
`coach_interview_answers`: **0 rows**. It is reachable: "Knowledge Base" is in the coach nav, and the
knowledge page leads with a "Train your coach" card showing `0/78`.

She named this her top priority and left the call saying "I'm on it". The blocker is not the build,
it is that nobody sent her the screenshot and the link. **This is the cheapest unblock on the list.**

### 12. Portal Spanish-default bug. Fixed and verified in production. Half of it remains.

**She said** [1:04:19]: "**Why is it in Spanish?**" **LaSean:** "It switched over. I don't know...
that's a quick bug. I'll fix that."

**Status: fixed, verified live, and it does address her path.** Commit `a1619c6` is deployed (`HEAD`
== `origin/main`, `/api/v1/ping` 200). Verified against production, not against the diff:

```
GET https://www.teamthickandfit.com/es/faq  ->  Set-Cookie: ui_locale=es; Path=/; SameSite=lax
GET https://www.teamthickandfit.com/faq     ->  Set-Cookie: ui_locale=en; Path=/; SameSite=lax
```

No `Max-Age` on either, so both are session cookies now. The old code wrote a **one-year** cookie on
any `/es` URL. That is exactly her path: she tapped "Español" in the marketing nav, landed on an
`/es` URL, and every signed-in page after that came up Spanish. Her existing stale cookie also
self-heals, because loading any English page now overwrites it.

**What is still broken:** once she is inside the coach portal in Spanish, there is no way out.
`LanguageToggle` is mounted on exactly two pages, `/account` and `/coach/settings`, as a `<select>`.
The coach shell has no language control. She asked "Why is it in Spanish?" on a live call and could
not fix it herself, and that part is unchanged.

### 13. Black and white. The pink-red is still everywhere, including live.

**She said** [08:07]: "I want to keep it all black and white. have noticed this **pink-red
situation**. I've decided, like, I want to keep it very just black and white." **Rodney** [09:00]:
"we can definitely go black and white... the banner and the buttons... we'll keep it very simplistic
and do a **solid color**."

**Status: not done.** `#ff2d55` appears in **22 source files**, including all three funnel pages,
`/about`, and all six `/vs/*` pages. Verified live: `https://www.teamthickandfit.com/join` serves
`ff2d55`.

Honest caveat: she was looking at Shakira's mockup when she said it. Nobody said our site was exempt,
it is the same colour, and the decision was stated as a brand rule ("I want to keep it all black and
white"), not as a note on one banner. Treat it as ours until she says otherwise. What is still
undecided is which single accent replaces it, because Rodney explicitly kept one for buttons.

### 14. "Cancel anytime, no contract" is unqualified across the site.

**She asked** [1:13:03]: "When we say cancel anytime, no contract, **we're only talking about the low
ticket, right?**" **LaSean:** "I'll clarify. We'll get it clarified."

**Status: not done.** The phrase appears in 14 strings in `en.json` plus `/vs/myfitnesspal`. The one
that matters is `tierTrialNote`, which renders directly beneath the onboarding tier picker that
offers Team and 1-on-1 with Steph. As written it promises no-contract terms on the high-ticket tiers
too. That is the kind of sentence that gets quoted back during a dispute.

### 15. Local currency. Not started, and the copy now says the opposite.

**She said** [17:24]: "remember how we decided **I wanted everything to be charged in their
currency**." **LaSean:** "that's not what I've been primarily focusing on right now."

**Status: not started.** Stripe sends no `currency_options`; `src/lib/billing/subscriptions.ts`
defaults to `usd` in four places. Meanwhile `en.json:2610` and `es.json:2610` say "**Same price in
every country**" / "El mismo precio en cada país", which is a public commitment against the thing she
asked for. One of the two has to go.

Her poll data from the call is not recorded anywhere in the repo: Caribbean 278 votes, Colombia 215,
South and Central America 243 + 94. She said she would send it. It should land in
`.planning/MARKETING-AUDIENCE.md`.

### 16. About page: credentials. Partly done, with a conflict that needs her answer.

**She said** [32:00]: "it's important to be able to solidify my actual, my certifications, knowing
that, hey, I'm a certified personal trainer. **I'm also a certified nutritionist.** And then also my
athletic background... And I think it's very important to be able to state that, you know, we're all
certified and so is my team." Plus [33:27]: "my team does have **pre and post-natal experience**."

**Status: partly, and one item is contested.** `src/app/about/page.tsx` carries "certified personal
trainer" and the founder food story. It deliberately does **not** say nutritionist: the file comment
records "never 'nutritionist'/'PN-certified' (she is not one...)". She said on this call that she is.
**Do not put it on a public page until she names the certifying body.** That is a claim regulators
and competitors both check.

Missing outright: her athletic background, her team's certifications, and the pre/post-natal
experience.

Do not add brand deals. **Stephanie** [32:54]: Women's Best "is up to renew at the end of September
and I do not know if I'm going to stay with them."

### 17. A domain email for the app. Still bounces.

**She said** [39:02]: "Am I able to get a... Domain with Team Thick and Fit?... for email purposes."
**LaSean:** "everything, support questions, everything about the app can run through that and no
longer your personal email... the app is going to be a separate business."

**Status: not done, and the codebase is already routing around it.** `hello@teamthickandfit.com` has
**no MX record and bounces**. Five files carry explicit workarounds (`privacy`, `terms`, `support`,
`legal-page`, `support-form`) and `RESEND_FROM` still defaults to it as a *from* address.
`/api/support/inbound` is written expecting `help@teamthickandfit.com` to be pointed at it in Resend;
that has not been done. This is a DNS task, not a build task.

### 18. Chargeback counter-evidence in her own portal.

**She said** [1:03:22]: "when a client disputes a charge, I'm going through this right now with
Linus. I don't have any ability to be able to counter it... Are we going to be able to like, be able
to counter it? **Is it going to be popping up on my end to where I can counter it and upload the
receipts?**"

**Status: not started.** `dispute` and `chargeback` appear in the Stripe webhook and billing lib, but
there is no coach or admin surface that shows an open dispute, and no evidence upload. She said yes
to this being answered and it was not answered on the call.

### 19. Stripe secret keys. Still the blocker it was on 2026-07-30.

**LaSean** [59:07]: "I'm going need you to go in and get my secret keys... we can carve out like 10
minutes."

**Status: outstanding, carried from the 2026-07-30 internal call.** Production has 1 `entitlements`
row and `isEntitled()` still short-circuits to `true` because `isStripeConfigured()` is false. **The
paywall is inert.** Nothing about pricing, tiers, founding windows or grandfathering can be tested
until this 10-minute call happens. It was scheduled and then dropped because both of them were going
to Atlanta.

### 20. Six-second in-app video loop on the funnel.

**Shakira** [05:12]: "the goal with this would be that we would be able to do an actual video loop of
the app inside of it... a six-second loop-ish."

**Status: not started.** Zero `<video>` elements on `/join`. Owner Shakira for the mock, us for the
real page.

### 21. New photos, not the old one.

**She said** [08:07]: "obviously the pictures, I want to use all new pictures that I sent in that
drive, **because that's an old picture**."

**Status: cannot verify from the repo.** Our `/join` hero is `public/brand/shoot/pink-front.avif`.
Thirteen shoot images are checked in. Somebody has to open the drive and diff it against
`public/brand/shoot/`. Flagging it rather than guessing.

---

## Owned by other people, tracked so it does not fall

| item | owner | state |
|---|---|---|
| Funnel copy, EN + ES, in a Google Doc for her review | Shakira | promised same night, outside this repo |
| A Spanish reader to check the translated copy | Shakira asked, unassigned | open |
| Colour-coded client list: renewal dates, amounts, transition notes | Stephanie | blocks the grandfathering build |
| Answer the 78 questions | Stephanie | 0 of 78 |
| Fanbase account for high-ticket financing | Stephanie to meet the president; LaSean to price the integration | open |
| UX training session, walk the app as a customer | Rodney | not scheduled |
| Storyline for the origin video | Stephanie | open |

---

## Decisions that need no build

- **PWA-first, App Store later.** Already true. `ios/` and `capacitor.config.ts` exist, the manifest
  and install button exist, and she installed the PWA live on the call.
- **Do not pay to expedite the D-U-N-S.** **LaSean** [45:00]: "You don't have to spend that money."
  Honoured. The App Store application has not been filed, which is correct under PWA-first.
- **Never present it as AI to members.** Already a standing rule in this codebase.

---

## Two of our own documents are now wrong and should be corrected before she sees them

1. **`.planning/GIVEAWAY-RULES-DRAFT.md`** asks her to confirm three drawing dates. The call says
   twice weekly, Tuesdays and Thursdays, escalating. Rewrite before it goes to her or to a lawyer.
2. **`CLAUDE.md`, Site Visibility** still names Sept 27 as doors and Aug 4 as the waitlist open. Both
   are superseded. The `PRELAUNCH_HIDE_SITE` row in particular reads as a live plan.

---

## One number worth staring at

Production `waitlist_leads`: **1 lead, 1 total entry.** Every entry rule, cap, referral credit and
draw weight above is untested against real traffic. Whatever we fix here, fix it before the traffic
arrives, not after.
