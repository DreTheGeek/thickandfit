// The evidence-based light build, kept reachable for comparison after the dark direction became
// the default. Noindexed: a decision aid, not a second public home page.
import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Thick & Fit (light preview)', robots: { index: false, follow: false } };
export { default } from '../../page';
