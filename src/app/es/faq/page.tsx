// Spanish FAQ. Q&A is the format answer engines surface most, so the Spanish version needs its own
// URL just as much as the English one.
export { default, generateMetadata } from '../../faq/page';
