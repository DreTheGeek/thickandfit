// Spanish About. The entity page matters most for AIO: it is what an engine reads to decide who
// Stephanie is, so it needs to exist in both languages.
export { default, generateMetadata } from '../../about/page';
